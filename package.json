{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --port 8080",
    "deploy": "vitepress build docs && cd .vitepress && git init && git add -A && git commit -m 'deploy' && git push -f git@github.com:JY-HE/JY-HE.github.io.git main"
  },
  "dependencies": {
    "sass": "^1.60.0"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-alpha.64"
  }
}
